# TomChat Configuration
# Named after Tommy 🐕

[hotkey]
# Configurable hotkey combination
combination = "caps"

[audio]
# Audio capture settings
sample_rate = 16000
channels = 1
buffer_duration_ms = 64  # Low latency

[vad]
# Voice Activity Detection settings  
sensitivity = "Normal"  # Low, Normal, High, VeryHigh
timeout_ms = 500       # Stop recording after silence

[whisper]
# Whisper model settings
model_path = "./models/ggml-small.en-q8_0.bin"
language = "en"
translate = false

[text]
# Text injection settings
typing_delay_ms = 1  # Delay between keystrokes
